git init 
git add FILEPATH
git commit -m "MESSAGE"
git config --global user.name "Umair"
git config --global user.email "EMAIL"
git remote add <NAME> <URL>
git branch -M main  
git checkout -b feat/add-commands
git checkout 
git push 
git pull
